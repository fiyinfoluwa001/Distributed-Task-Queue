
PORT=3000
NODE_ENV=development

DATABASE_URL=mysql://fiyinfoluwa:fiyinfoluwa@localhost:3306/distributed-task-queue

REDIS_HOST=localhost
REDIS_PORT=6379

JWT_SECRET=change-me-in-production
JWT_EXPIRATION=1d

# SMTP — leave blank to disable email notifications
# Any SMTP provider works (Gmail, SendGrid, Resend, Mailgun, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@your-app.com
